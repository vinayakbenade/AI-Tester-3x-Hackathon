# LLM API key — get from https://console.groq.com/keys
# Never prefix with NEXT_PUBLIC_ — that exposes it to the browser bundle.
GROQ_API_KEY=your-groq-api-key-here

# Manual baseline minutes per entry for time-saved calculation (optional, default 15)
MANUAL_BASELINE_MINUTES=15
